import { NextRequest, NextResponse } from 'next/server'
export async function POST(req: NextRequest){const body=await req.formData();const name=String(body.get('name')||'');const email=String(body.get('email')||'');const message=String(body.get('message')||'');if(!name||!email||!message){return NextResponse.json({ok:false,error:'Missing fields'},{status:400})}const mailto=`mailto:info@modendevelopment.gr?subject=New%20inquiry%20from%20moden.gr&body=${encodeURIComponent(`Name: ${name}\nEmail: ${email}\n\n${message}`)}`;return NextResponse.redirect(mailto)}